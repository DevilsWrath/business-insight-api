services:
  db:
    image: postgres:15
    container_name: businessinsight-db
    environment:
      POSTGRES_DB: businessinsight
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data: